node_modules
.next

# Superpowers brainstorming scratch (mockups, server state, transcripts)
.superpowers/

# Env files with secrets — never commit
.env
.env.local
.env.*.local
.env.test
.env.development
.env.production
# Keep example as the only committed env template
!.env.example
