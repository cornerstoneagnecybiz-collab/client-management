# Resolve peer dependency conflict between next@15.0.3 (expects React 18 or specific 19 RC) and react@^19.0.0.
# Required for npm install to succeed on Vercel and locally without --legacy-peer-deps.
legacy-peer-deps=true
