# Copy to .env or .env.local and fill with your values. Never commit .env or .env.test.
# If .env was ever committed to git, rotate all Supabase keys in the dashboard and revoke old ones.
#
# Supabase (create project at supabase.com)
# Get values from: https://supabase.com/dashboard/project/_/settings/api
# Required: URL and one public key (NEXT_PUBLIC_* so the app can run in the browser).

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Public key — use exactly one of these names (app reads in this order):
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# NEXT_PUBLIC_SUPABASE_KEY=your-publishable-or-anon-key

# Optional: server-only (bypasses RLS). Never expose to client.
# SUPABASE_SECRET_KEY=your-service-role-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
