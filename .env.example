# Supabase (create project at supabase.com)
# Both URL and key must be set. Key must use a NEXT_PUBLIC_ name so the app can run in the browser.
# Get values from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Use one of these (Publishable or anon key from API settings):
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: server-only admin operations (bypasses RLS). Never expose to client.
# SUPABASE_SECRET_KEY=your-secret-or-service-role-key
